Pour cet exercice, une base de donnée MySQL à été utilisée,
Les données sont structurées sous ce format : id/username/password/email (id s'autoincrémente)

Pour le GUI je suis allé aussi loin que possible mais les deux méthodes que j'ai essayé afin d'obtenir le résultat
escompté ont étés des échecs :

Supprimer le pannel au clic de l'utilisateur sur le menu pour le replacer un autre ne marche pas et fait juste
bugger le programme (aucune action ne marche plus)
Et la méthode actuellement implémentée ne fait rien non plus et ne peux qu'afficher le panel d'ajout d'utilisateur (fonctionnel)